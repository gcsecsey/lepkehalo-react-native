import { StyleSheet } from 'react-native';

export const Headings = StyleSheet.create({
  h1: {
    fontSize: 18,
    fontWeight: 'bold',
  },
  h2: {
    fontSize: 16,
    fontWeight: 'bold',
  },
  subheading: {
    fontSize: 16,
  },
});
